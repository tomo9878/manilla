# インパルス型アクションシステム (Impulse Action System) 設計書の更新

## 1. 概要
従来の「全移動フェーズ終了後に全戦闘フェーズ」という厳格な区分を廃止し、プレイヤーが任意のタイミングで移動と戦闘を行える**インパルス型（逐次処理型）システム**へ移行する。
これにより、「Ａ地点での戦闘結果を見てから、Ｂ地点へ増援を送る」といった柔軟な戦力運用（帳尻合わせ）が可能になる。

## 2. フェーズ構成の変更

### 旧構成
Supply -> **Movement** (全移動) -> **Combat** (全戦闘) -> End

### 新構成
Supply -> **Action Phase** (移動と戦闘を自由に繰り返し) -> End

*   **Action Phase** では、以下の行動を好きな順序・組み合わせで行える。
    *   ユニットの移動（未行動ユニットのみ）
    *   敵地への進入（Contested化）
    *   戦闘の解決（任意のタイミング）

## 3. インタラクション詳細

### A. 移動 (Movement)
*   **操作**: クリック選択（Click-to-Move）またはドラッグ＆ドロップ。
*   **制限**: `Fresh`（表面/未行動）のユニットのみ移動可能。
*   **敵地進入**: 日本軍（または未確認ユニット）がいるエリアへ移動した場合、そのエリアは**「係争中 (Contested)」**となる。
    *   マップ上に `⚔️` マーカーが表示される（視覚的な目印として）。

### B. 戦闘開始 (Combat Initiation) - 右クリックメニュー採用
*   **トリガー**: プレイヤーがエリア（または係争地マーカー、ユニットスタック）を**右クリック (Context Menu)** する。
*   **メニュー項目**:
    *   **"Resolve Combat (戦闘開始)"**:
        *   条件: そのエリアに「自分(US)のユニット」と「敵(JP)のユニット」が両方存在する場合のみ有効化。
    *   (既存の "Recover" や "Out of Action" 等も維持)
*   **処理フロー**:
    1.  右クリック -> "Resolve Combat" を選択。
    2.  `CombatModal` が開く。
    3.  戦闘解決（ダイスロール）。
    4.  **結果適用**:
        *   戦闘に参加したUSユニットは `Spent`（行動済み/裏面）になる。
        *   敵ユニットが除去されたら、そのエリアは「US支配下」等の通常状態に戻る。
        *   敵が残った場合、次のターン（または別の未行動ユニットによる第2波攻撃）まで対峙が続く。

### C. ターンの終了
*   プレイヤーが画面右パネル等の**「End Action Phase」**ボタンを押すとフェーズ終了。
*   未行動のユニットが残っていても終了可能（あえて温存するなど）。
*   警告: 「まだ係争中のエリア（解決していない戦闘）がありますが、終了しますか？」等のダイアログを出すと親切。

## 4. 実装タスク

1.  **フェーズ管理の統合**:
    *   `App.jsx` の `currentPhase` を `'Movement'`, `'Combat'` から `'Action'` へ統合（または `'Action'` で統一）。
    *   UIのフェーズ進行ボタンを整理。

2.  **エリア右クリックメニューの拡張**:
    *   `handleAreaContextMenu` (または既存の `handleUnitContextMenu`) に戦闘開始オプションを追加。
    *   クリック対象が「エリア背景」でも反応するように `Line` (エリア描画) コンポーネントに `onContextMenu` を追加。

3.  **戦闘モーダルの呼び出し**:
    *   メニュー選択時に `handleCombatInitiation(areaName)` を実行。
    *   フェーズチェック (`currentPhase === 'Combat'`) を外し、`'Action'` フェーズならいつでも可能にする。

4.  **事後処理**:
    *   戦闘に参加したユニットを即座に `Spent` にする処理は既存 (`CombatModal` -> `handleCombatApply`) を流用・確認。
    
## 5. UI/UXのメリット
*   **直感的**: 「あ、ここ攻撃しなきゃ」と思った場所を右クリックですぐ始められる。
*   **柔軟性**: 移動だけして寸止め（包囲だけする）、移動して即攻撃、など自由自在。
*   **誤操作防止**: 「移動フェーズ終了ボタン」の誤クリックによる事故がなくなる（いつでも移動・攻撃できるため）。
