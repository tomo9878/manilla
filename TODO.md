# Manila: Savage Streets, 1945 - 開発ロードマップ

## フェーズ1: 視覚・操作基盤の構築 (UI/UX)
**目標**: まずはルール等の判定は行わず、地図上でコマを自由に配置・移動できる「サンドボックス（箱庭）」状態を作る。

### 1.1 ユニットとアセットのシステム
- [ ] **画像の読み込み**: `buildFile.xml` に記載されているユニット画像（カウンター）をフロントエンドで表示できるようにする。
- [ ] **ユニットコンポーネント作成**: Reactでユニットを表示する共通部品を作る（表面 / 裏面 / 負傷面 の切り替えロジック含む）。
- [ ] **スタック（重なり）の可視化**: 1つのエリアに複数のユニットがある場合の表示処理。
    - [ ] マウスホバーやクリックで重なっているユニットを展開して表示する「スタックビュー」。
    - [ ] ユニットが何枚重なっているかの数値バッジ表示。

### 1.2 盤面操作
- [ ] **初期配置の描画**: 「PredefinedSetup」（初期配置データ）を読み込み、ユニットを正しい開始エリアに配置する。
- [ ] **ドラッグ＆ドロップ移動**: ドラッグ操作でエリアからエリアへユニットを移動できるようにする。
    - [ ] `map_data.json` の座標を使って、ドロップ時にエリアの中心へ綺麗に吸着させる。
- [ ] **状態更新**: フロントエンド内部で「どのエリアにどのユニットがいるか」というリストを管理・更新できるようにする。

### 1.3 マーカー・情報表示
- [ ] **支配マーカー**: エリアごとの「アメリカ軍支配」「日本軍支配」を切り替える視覚的なトグル機能。
- [ ] **ゲームトラック表示**: マップ右側にある記録トラックの実装：
    - [ ] ターン記録（Turn 1-9）
    - [ ] 勝利ポイント（VP）トラック
    - [ ] インパルス/フェイズ記録

---

## フェーズ2: ゲームロジックとルール実装 (Backend/FastAPI)
**目標**: サーバー側で厳格なルール判定を行い、移動可否や戦闘結果、チットプル（行動順決定）を管理する。

### 2.1 ゲーム状態モデルの構築 (Backend)
- [ ] **状態スキーマ定義**: Python (Pydantic) でゲーム全体のデータ構造を定義する。
    - `GameState`: 現在のターン、フェイズ、活性化しているチット。
    - `MapState`: 各エリアの支配状況、存在するユニットリスト。
    - `UnitState`: ID、兵科、現在の戦力（ステップ数）、状態（活動済み/混乱など）。
- [ ] **APIエンドポイント実装**:
    - `GET /game/state`: 現在の盤面情報を取得。
    - `POST /game/action`: プレイヤーの操作（移動、戦闘宣言など）を送信。

### 2.2 コアメカニクス: チットプルシステム
- [ ] **カップ（デッキ）のロジック**: ターンごとに「カップに入れるべきチット（活性化マーカー）」を管理する仕組み。
    - 例：「第37歩兵師団チット」や「日本軍イベントチット」などをターン数に応じて追加。
- [ ] **ドロー処理**: ランダムにチットを引くアクションの実装。
- [ ] **インパルス管理**: 引いたチットに応じて、どのユニットが行動可能かを判定するロジック。

### 2.3 移動ルール
- [ ] **隣接情報の定義**: `map_data.json` を拡張し、「どのエリアとどのエリアがつながっているか」というグラフデータを作成する。
- [ ] **移動コスト判定**: 地形（Clear/Urban/Fort）に応じた移動力の消費ルールを実装する。
- [ ] **スタック制限**: 1エリアに置けるユニット数の上限ルールを適用する。

### 2.4 戦闘エンジン
- [ ] **戦闘結果表 (CRT) の実装**: サイコロの出目と戦力差による結果テーブルをコード化する。
- [ ] **戦闘解決処理**:
    - 入力: 攻撃側ユニット群、防御側ユニット群、地形補正、支援カード。
    - 処理: オッズ（戦力比）計算 -> ダイスロール (1d6) -> CRT参照。
    - 出力: 損害（ステップロス）、退却、前進。
- [ ] **ユニット状態更新**: 戦闘結果に基づき、ユニットを裏返す（ステップロス）か、除去（Out of Action）する。

### 2.5 勝利条件
- [ ] **VP自動計算**: 支配しているエリアのVP値を合計し、現在のスコアを算出する。
- [ ] **終了判定**: 最終ターン終了時、またはサドンデス条件による勝敗判定。

---

## フェーズ3: 仕上げとマルチプレイ
- [ ] **ログ・履歴表示**: 「米軍がエリア5へ移動」「戦闘発生: ダイス4で損害なし」といったアクションログを表示する。
- [ ] **セーブ/ロード**: ゲームの状態をファイルやDBに保存・復元できるようにする。
- [ ] **WebSocket通信**: 対戦相手のブラウザにリアルタイムで動きを反映させる（完全なオンライン対戦化）。
