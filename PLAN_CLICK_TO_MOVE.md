# クリック式移動システム (Click-to-Move) 詳細設計図

## 1. 概要
ドラッグ&ドロップ操作に加え、より正確で直感的な「クリック選択式」のユニット移動システムを導入する。
密集したエリア間での誤操作（マウズドロップ）を防ぎ、移動可能なエリアのみを視覚的に提示することでプレイアビリティを向上させる。

## 2. インタラクションの流れ (Interaction Flow)

### Step 1: ユニットの選択 (Unit Focus)
*   **アクション**: プレイヤーが `Fresh`（表面）状態のユニットを左クリック。
*   **UI変化**:
    *   選択されたユニットがハイライト表示（枠線、明度アップ等）される。
    *   他ユニットとの区別を明確にする。

### Step 2: 移動先候補の提示 (Destination Options)
*   **アクション**: システムが選択ユニットの現在地に基づき、隣接エリアを計算。
*   **UI変化**:
    *   ユニット近傍または該当エリア上に「移動先選択肢」を表示（吹き出し、またはエリア自体のハイライト）。
*   **フィルタリング**:
    *   隣接していても移動不可能なエリア（スタック制限超過、地形不可）は、無効化（グレーアウト）または非表示にする。

### Step 3: 移動の実行 (Execution)
*   **アクション**: プレイヤーが表示された「移動先エリア」をクリック。
*   **システム処理**:
    *   ユニットが対象エリアへアニメーション移動。
    *   バックエンドの状態（`location`, `x`, `y`）を更新。
    *   移動完了としてステータスを `Spent`（裏面）に変更するか、MF消費ロジックに従う。

## 3. ロジック・チェックリスト（バリデーション）

移動先リストを表示する前に、以下の判定を行う。

### A. 除外条件 (Exclude) - リストに表示しない
1.  **非隣接 (No Adjacency)**:
    *   隣接データ（`adjacency.json`等）に含まれていないエリア。
2.  **進入不可 (Impassable)**:
    *   川（River）などの地形制限により移動できないエリア。
3.  **移動力不足 (Insufficient MF)**:
    *   （将来的な実装）残りMFで到達できないエリア。

### B. 無効化条件 (Disable/Gray-out) - 表示するが選択不可
1.  **スタック制限超過 (Stacking Limit)**:
    *   移動先に既に **6個** の米軍ユニットが存在する場合。
    *   UI: 「Area X (満員)」等の表示を行う。

## 4. 技術的実装方針

### データ構造
*   **隣接データ**: `generate_adjacency.py` 等を用いて、各エリアの隣接情報を事前に生成する（またはリアルタイム計算）。
    *   フォーマット例: `{"Area 1": ["Area 2", "Area 5"], ...}`

### フロントエンド `App.jsx`
*   **State追加**:
    *   `selectedUnitId`: 現在選択中のユニットID。
    *   `movementOptions`: 計算された移動可能エリアのリスト。
*   **ハンドラ**:
    *   `handleUnitClick`: 選択処理および候補算出。
    *   `handleMoveSelect`: 移動実行。

## 5. 次のステップ
1.  隣接データ (`generate_adjacency.py`) の検証と適用。
2.  `App.jsx` へのクリック移動ロジックの実装。
